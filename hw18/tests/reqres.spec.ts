import {expect} from "@jest/globals";

describe("Testing HTTP methods", () => {
    test ("Should correctly read GET response",() => {
        expect();
    })

    test ("Should ",() => {
        expect();
    })

    test ("",() => {
        expect();
    })
})